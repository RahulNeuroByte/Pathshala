# 🌐 API CONFIG (FASTAPI BACKEND)
# Use relative path /api/v1 to leverage the Vite proxy (dev) and Nginx proxy (production/docker)
VITE_API_URL=/api/v1

# 🔐 AUTH CONFIG
VITE_TOKEN_KEY=access_token

# 🏫 APP CONFIG
VITE_APP_NAME=Pathshala ERP
VITE_APP_VERSION=1.0.0
VITE_ENV=development
